# ♠ 텍사스 홀덤

AI 상대와 혼자 즐기는 노리밋 텍사스 홀덤.
빌드 도구도, 외부 의존성도 없습니다. `index.html` 을 브라우저로 열면 바로 시작됩니다.

```bash
open index.html            # macOS
xdg-open index.html        # Linux

npm start                  # 또는 로컬 서버 (http://localhost:8000)
```

로컬 서버로 열면 오프라인 설치(PWA)와 Web Worker 가 함께 켜집니다.
`file://` 로 열어도 워커는 Blob URL 로 동작합니다.

---

## 무엇이 다른가

무료 홀덤 앱은 많지만 대부분 봇이 약합니다. 이 프로젝트는 **봇이 실제로 강한 것**과
**플레이어의 실력이 느는 것**, 두 가지를 목표로 만들었습니다.

### 봇이 실제로 강하다

봇은 매 결정마다 상대의 액션에서 **레인지를 역산**하고, 그 레인지로만 승률을
시뮬레이션합니다. "상대는 무작위 홀카드"라는 흔한 가정은 실측 결과 최대 **23.8%p**
까지 틀립니다(포켓 99: 랜덤 상대 72.8% vs 상위 15% 레인지 49.3%).

| 측정 | 초기 구현 | 현재 |
|---|---|---|
| 고정 규칙 TAG 봇 상대 손익 (6시드 × 1000핸드) | **-149 bb/100** | **+8 bb/100** (고급) · **+6 bb/100** (보통) |
| 봇 VPIP / PFR | 48~58% / 0~8% (루즈-패시브 림퍼) | 30% / 18% (AF 2.2) |
| 핸드 평가기 처리량 | 47,000 회/초 | **288만 회/초** |
| 결정 1회 비용 | 29~69ms (UI 정지) | 0.4~1.3ms (+ 워커) |

TAG 봇은 `test/tag-bot.js` 에 고정 규칙으로 들어 있고, `npm run bench` 가 6시드 × 1000핸드로
평균과 표준오차(약 ±6~9 bb/100)를 냅니다. 단위 테스트에는 큰 퇴행만 잡는 안전망(800핸드
한 시드)이 들어 있습니다. 초기 구현의 수치는 커밋되지 않은 30줄짜리 TAG 로 잰 것이라 지금
봇과 직접 비교할 수 없고, 지금 봇은 팟 오즈로 드로우를 콜하고 C벳을 하는 만큼 더 강합니다.

### 실력이 느는 도구

- **핸드 리뷰** — 내 결정 하나하나를 EV 로 채점합니다.
  "승률 18%, 필요 승률 25% → 폴드가 맞습니다 (-12칩)"
- **아웃 카운터** — 보드 위 가능한 모든 홀카드 조합의 강도 분포를 실제로 계산해
  아웃을 셉니다. 보드 페어처럼 "나만 좋아지는 게 아닌" 카드는 자동으로 빠집니다.
- **프리플랍 레인지 차트** — 13×13 격자로 포지션별 오픈 레인지와 내 핸드의 위치
- **세션 통계** — VPIP / PFR / AF / 3벳 / WTSD / bb per 100 + 칩 추이 그래프
- **핸드 히스토리와 리플레이** — 지나간 핸드를 스트리트 단위로 되감기
- **AI 속마음** — 봇이 왜 그렇게 했는지 (승률 추정, 블러프 여부, 폴드 기대치)
- **스트리트별 액션 요약** — 이번 핸드에서 내가 어떤 라인을 탔는지 한 줄로
- **약점 프로파일** — 리뷰의 결정들을 자리(스트리트 × 상황 12종)와 포지션별로
  세션을 넘어 쌓아 "어디서 가장 많이 흘리는지"를 보여줍니다. 표본 5개 미만인
  자리는 약점이라 부르지 않습니다.
- **드릴 모드** — 약한 자리만 골라 반복 출제합니다. 봇들로 핸드를 진행시키다
  내가 그 자리에 서는 순간 멈추고 묻고, 답하면 리뷰와 같은 기준으로 채점하고
  상대 패를 공개합니다. 그 자리에 이르기까지의 내 앞선 결정은 고급 AI 가 대신 둡니다.

---

## 기능

**게임** · 노리밋 홀덤 정식 룰 / 상대 1~5명 / 사이드 팟 / 앤티 3종
(없음·빅블라인드 앤티·전원 앤티) / 블라인드 레벨 자동 상승 / 리바이·애드온 /
탈락 등수 / ICM 지분과 압박 표시 / 머크·쇼 선택 / 액션 클락과 타임뱅크

**AI** · 난이도 3단계 / 포지션 인식 / 베팅 사이즈 리딩 / 폴드 에쿼티 기반 블러프 /
상대 프로파일링(고급) / 성향 5종(타이트·밸런스·어그레시브·콜링스테이션·트래퍼)

**베팅** · 상황별 스마트 벳 버튼(오픈 2x·2.5x·3x / 레이즈 직면 2.5x·3x·4x / 포스트플랍 33·50·75·100·150%) /
금액 직접 입력(숫자 키패드) / 슬라이더 / 시작 칩에 bb 깊이 병기 /
**칩 ↔ bb 표시 전환**(상단 버튼 · 팟·스택·벳·버튼·로그·리뷰·차트가 함께 바뀌고 입력칸도 bb 로 받는다)

**화면** · 코드로 그린 카드와 칩 / 승리 5장 하이라이트 / 덱에서 날아오는 딜링 모션 / 칩 이동·카드 뒤집기 애니메이션 /
4색 덱 / 아바타 표정 / 사이드팟 시각화 / 스트리트별 액션 요약 /
모바일 세로·가로 대응 / 햅틱 / 효과음

**기타** · 한국어·영어 / 진행 중인 게임 저장·복원 / 시드 고정(같은 카드 재현) /
오프라인 설치(PWA) / 키보드 조작 / 스크린리더 대응

### 단축키

| 키 | 동작 |
|---|---|
| `F` | 폴드 |
| `C` | 체크 / 콜 |
| `R` | 레이즈 |
| `V` | 핸드 리뷰 |
| `Space` | 다음 핸드 |
| `←` `→` | 리플레이 이동 |
| `Esc` | 모달 닫기 |

---

## AI 는 어떻게 판단하나

**1. 상대 레인지를 역산한다.**
레인지는 169개 핸드 클래스마다 0~1 의 **가중치**입니다. "상위 N%" 밴드 하나로는
같은 백분위의 수티드 커넥터와 약한 오프수트 에이스를 구별할 수 없어서, 액션마다
모양을 다르게 줍니다 — 오픈은 포지션별 상위 14~44% 에 경계를 부드럽게 기울이고,
3벳은 프리미엄에 수티드 에이스·커넥터 블러프가 섞이고, 콜은 포켓페어와 수티드가
앞섭니다. 보드가 깔리면 **가능한 모든 홀카드 조합**(플랍이면 1,081가지)을 실제로
평가해 강도 분포를 만들고, 베팅 사이즈에 따라 그 분포 위에서 가중치를 다시 줍니다.
하드 컷이 아니라 소프트 컷이라 약한 꼬리에 블러프·플로트 몫이 남습니다. 팟의 30% 를
친 상대와 120% 를 친 상대는 다른 레인지입니다. 가중치는 콤보 복제 수로 양자화되어
시뮬레이션 커널은 균등 샘플링 그대로입니다. 내가 든 카드는 상대 콤보에서 빠지므로
블로커 효과가 자동으로 따라옵니다.

**2. 그 레인지로만 승률을 시뮬레이션한다.**
덱에서 아무 카드나 뽑지 않고, 레인지에 속한 조합만 샘플링합니다.

**3. 선택지마다 EV 를 계산해 비교한다.**
폴드·체크·콜, 그리고 여러 베팅 사이즈 각각에 대해:

```
EV(벳 b) = P(전원 폴드) × 팟
         + P(콜) × [ 콜당했을 때의 승률 × (팟 + 콜러수 × b) − (1−승률) × 내 비용 ]
```

폴드 확률은 MDF(최소 방어 빈도)에서 출발해 상대의 실제 폴드 성향으로 보정하고,
**"콜당했을 때의 승률"은 따로 계산합니다** — 상대는 좋은 패로만 콜하니까요.

**4. 블러프는 EV 마진으로 통제한다.**
EV 추정에는 오차가 있으므로, 블러프는 최선의 비블러프 선택지를 일정 마진 이상
이길 때만 실행합니다. 공격적인 성향일수록 마진이 작습니다.

**5. 고급 난이도는 당신을 학습한다.**
포스트플랍 폴드율이 높으면 블러프를 늘리고, 콜이 잦으면 블러프를 끊고 밸류만
칩니다. VPIP 로 당신의 레인지 폭을 보정합니다.

### 개발 중 만난 함정 두 가지

코드 주석에 남겨 두었습니다.

1. **상대 모델링용 레인지를 내 콜 기준으로 재사용하면 안 된다.**
   모델링용 레인지는 넓게 잡는 게 맞지만, 그걸 그대로 콜 기준으로 쓰면
   빅블라인드가 85% 를 디펜스하게 되어 VPIP 가 60% 로 치솟습니다.
2. **프로파일링에 전체 폴드율을 쓰면 안 된다.**
   프리플랍에서 나쁜 패를 접는 것까지 섞여 누구나 70% 안팎이 나오고, 봇이
   "뭐든 접는 상대"로 오판해 블러프를 남발합니다(-69 → +231bb/100).
   블러프가 통할지는 **포스트플랍 폴드율**만이 말해 줍니다.

---

## 디자인 — 에셋 없이 코드로

카드 이미지 팩을 쓰지 않았습니다. 대신 **카드·칩·테이블을 전부 코드로 그립니다.**

**카드** (`js/cardart.js`) — 무늬 ♠♥♦♣ 는 SVG 패스로 직접 그리고, 코너 인덱스와
A/2~10 의 표준 핍 배치, J·Q·K 의 모노그램 프레임을 생성합니다. 어느 해상도에서나
선명하고, 4색 덱이 자동으로 따라오며, 작은 카드(봇 패)는 큰 인덱스 + 큰 무늬의
compact 배치로 바뀝니다.

**칩** — 금액대별 색(5 회색 · 25 초록 · 100 파랑 · 500 보라 · 1000 금)과 엣지 스팟,
스택 높이를 SVG 로 만듭니다. 베팅 배지와 날아가는 칩이 같은 그래픽을 씁니다.

**테이블** — `feTurbulence` 노이즈를 data URI 로 넣어 펠트 질감을 만들고, 가죽 레일과
스티칭, 림 라이트, 안쪽 비네트로 깊이를 줍니다.

왜 이렇게 했는지 — 대표적인 오픈 카드 팩(SVG-cards, LGPL-2.1)을 실제로 받아 뜯어보니
카드 한 장의 `<g>` 는 1 KB 남짓인데 전부 `<defs>` 참조라, **4장만 쓰든 52장을 쓰든
938 KB 를 통째로 실어야** 했습니다. `file://` 에서는 외부 SVG `<use>` 가 CORS 로
막히니 HTML 에 인라인해야 하고, 그러면 앱이 392 KB 에서 1.3 MB 로 불어납니다.
게다가 그 팩의 인물 카드 그림은 이 앱의 어두운 화면과 톤이 맞지 않았습니다.

**폰트만 예외입니다.** 타이포그래피는 코드로 그릴 수 없으니까요.
[Pretendard](https://github.com/orioncactus/pretendard) Variable(2.0 MB)에서
앱이 실제로 쓰는 657자만 서브셋해 **114 KB** 로 줄였고, 가변 축을 유지해 파일 하나로
모든 굵기를 씁니다. `@font-face` 의 외부 URL 은 `file://` 에서 CORS 로 차단되므로
`css/font.css` 에 data URI 로 내장했습니다. 라이선스는 SIL OFL 1.1.

참고로 Pretendard 에는 ♠♦♣ 글리프가 아예 없습니다. 무늬를 SVG 로 그린 이유이기도 합니다.

## 구조

```
index.html            화면 구성
css/font.css          Pretendard 서브셋 (data URI)
css/style.css         스타일
fonts/                폰트 원본·라이선스·서브셋 문자 목록
manifest.webmanifest  PWA 매니페스트
sw.js                 서비스 워커 (오프라인)

js/cards.js           카드 · 덱 · 셔플
js/cardart.js         카드/칩 그래픽 (SVG 생성)
js/i18n.js            한국어/영어 문자열
js/format.js          금액 표시 단위 (칩 / bb)
js/rng.js             시드 RNG (내부 상태 저장·복원 가능)
js/evaluator.js       핸드 평가기 (무할당 직접 평가)
js/ranges.js          169개 프리플랍 핸드 순위 · 포지션별 레인지
js/equity.js          레인지 기반 승률 · 폴드 에쿼티 · 아웃 · Web Worker
js/stats.js           VPIP/PFR/AF 등 통계 추적
js/tournament.js      블라인드 구조 · 상금 · ICM
js/engine.js          게임 엔진 (규칙 전체)
js/ai.js              AI 의사결정
js/review.js          핸드 리뷰 (EV 채점) · 결정 자리 분류
js/profile.js         약점 프로파일 (자리·포지션별 누적, localStorage)
js/drill.js           드릴 모드 (목표 자리 생성 · 채점)
js/history.js         핸드 기록 · 리플레이 · 내보내기
js/storage.js         설정 · 진행 중인 게임 보관
js/panels.js          통계/히스토리/차트/리뷰/리플레이 패널
js/ui.js              렌더링과 게임 루프

test/run-tests.js     단위 테스트 (의존성 없음)
test/tag-bot.js       벤치마크용 고정 규칙 TAG 봇
test/e2e.js           브라우저 E2E (Playwright)
```

엔진은 화면에 대해 아무것도 모릅니다. `phase` 하나로 다음 단계를 알려줍니다.

| phase | 다음에 할 일 |
|---|---|
| `awaiting-action` | 현재 차례 플레이어의 액션을 받는다 |
| `need-street` | `dealNextStreet()` |
| `showdown` | `resolveShowdown()` |
| `show-choice` | `chooseShow(boolean)` — 무쇼다운 승자의 공개/머크 |
| `hand-over` | `startHand()` |
| `game-over` | 한 명만 남음 |

### 핸드 평가기

7장에서 5장을 뽑는 21가지 조합을 모두 평가하는 대신, 랭크 카운트 + 무늬
비트마스크 + 8,192엔트리 스트레이트 룩업 테이블(8KB)로 한 번에 평가합니다.
객체를 전혀 만들지 않아 브루트포스 대비 **64배** 빠릅니다. 독립적인 레퍼런스
구현과 7장 20,000회 점수가 완전히 일치하는지 테스트에서 매번 확인합니다.

커널은 외부 스코프에 의존하지 않도록 작성되어 있어 `toString()` 으로 직렬화해
Web Worker 안에서 그대로 재생성됩니다. 그래서 `file://` 에서도 워커가 뜹니다.

---

## 테스트

```bash
npm test           # 단위 테스트 125개 (약 20초, 의존성 없음)
npm run bench      # AI 강도 벤치마크 (TAG 봇 상대, 6시드 × 1000핸드, 약 70초)
npm run lint       # ESLint
npm run test:e2e   # 브라우저 E2E 64개 (Playwright 필요)
npm run test:sw    # 서비스 워커 갱신 7개 (Playwright 필요)
```

단위 테스트가 확인하는 것: 평가기가 레퍼런스와 완전히 일치하는지, 휠 스트레이트와
킥커, 베팅 규칙(최소 레이즈·BB 옵션·포지션·정식 레이즈에 못 미치는 올인은 레이즈를
다시 열지 않음), 사이드 팟과 칩 총량 보존, 앤티,
블라인드 레벨, 리바이·애드온, ICM, 머크 규칙, 액션 클락, 레인지 역산,
아웃 카운터가 교과서 값과
맞는지, 클래스별 가중치 레인지(경계 기울기 · 3벳 블러프 · 콜 레인지 모양 · 콤보 복제 비율 ·
소프트 컷 · 블로커), **봇의 플레이 스타일이 현실적인 범위에 머무는지**, **난이도가 높을수록
실제로 강한지**, **고정 규칙 TAG 봇을 상대로 한 bb/100 이 하한 아래로 떨어지지 않는지**, 리뷰 판정 방향, 리플레이 팟의 단조 증가, 시드 재현성, i18n 키 누락, 결정 자리 분류,
약점 프로파일의 집계·정렬·왕복, 드릴이 12개 자리 전부를 만들어 내는지.

E2E 가 확인하는 것: 실제 브라우저에서의 진행, 워커 기동, 패널 4종 렌더링,
스마트 벳 버튼(상황별 종류와 금액 공식 · 직접 입력과 범위 보정 · 올인 프리셋이
스택이 step 의 배수가 아니어도 정확히 올인되는지), 칩 ↔ bb 전환(팟·칩·버튼·입력칸·과거 로그),
차트 툴팁, 리플레이, 학습 탭과 드릴(출제 → 채점 → 다음 문제 → 종료 후 실전 이어하기),
저장·복원 후 같은 보드가 이어지는지, 언어 전환,
접근성 속성, 좌석에 봇 성향이 새지 않는지, 손익 합계가 제로섬인지,
모바일 세로·가로에서 좌석이 테이블을 벗어나지 않는지.

서비스 워커 테스트가 확인하는 것: 한 번 접속한 사람에게 **새 배포가 도달하는지**.
로컬 서버를 띄우고 배포를 두 번 흉내 내, 옛 캐시가 지워지고 새 코드가 실제로
실행되는지까지 본다. `file://` 에서는 서비스 워커가 등록되지 않아 E2E 와 따로 둔다.

---

## 배포

GitHub Pages 로 나갑니다. 기본 브랜치에 푸시하면 `.github/workflows/pages.yml`
이 정적 파일만 추려 올립니다. 워크플로는 브랜치 이름을 박아 두지 않고 저장소 설정의
기본 브랜치를 따르므로, 기본 브랜치를 바꿔도 손댈 것이 없습니다. 다른 브랜치의 푸시는
빌드 단계에서 건너뜁니다.

한 가지 장치가 있습니다. 브라우저는 `sw.js` 의 바이트가 달라져야 서비스 워커의
`install` 을 다시 돌립니다. 그런데 `sw.js` 는 배포해도 늘 같은 파일이라, 예전에는
한 번 접속한 사람의 캐시가 첫 방문 때 받은 셸에 영영 멈춰 있었습니다. 고쳐 올려도
남의 화면은 그대로였습니다.

그래서 워크플로가 배포마다 커밋 SHA 를 `sw.js` 에 박습니다.

```yaml
sed -i "s/^const BUILD = 'dev';$/const BUILD = '${GITHUB_SHA}';/" site/sw.js
grep -q "const BUILD = '${GITHUB_SHA}';" site/sw.js || exit 1
```

캐시 이름이 배포마다 달라지므로 셸이 한 벌씩 통째로 갈립니다. 새 `index.html` 과
낡은 `js` 가 섞이지 않고, 옛 캐시는 `activate` 에서 지워집니다. `sed` 가 먹지
않으면 배포를 실패시킵니다 — 조용히 넘어가면 옛 버그가 그대로 돌아오기 때문입니다.

이미 접속했던 사람에게는 **다음 방문에** 새 버전이 뜹니다. 지금 열려 있는 탭을
강제로 새로고침하지는 않습니다 (핸드 중에 화면이 날아가면 곤란하니까요).

---

## 라이선스

MIT
